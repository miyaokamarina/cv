// @flow

import * as React from 'react';

export const RecordButton = () => (
    <img
        style={{
            width: '1.25em',
            verticalAlign: '-0.25em',
        }}
        alt={'⏺️'}
        aria-label={'Emoji: Record button'}
        title={'Record button'}
        src={'https://twemoji.maxcdn.com/2/72x72/23fa.png'}
        draggable={false}
    />
);
