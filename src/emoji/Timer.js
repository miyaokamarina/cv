// @flow

import * as React from 'react';

export const Timer = () => (
    <img
        style={{
            width: '1.25em',
            verticalAlign: '-0.25em',
        }}
        alt={'⏲️'}
        aria-label={'Emoji: Timer clock'}
        title={'Timer clock'}
        src={'https://twemoji.maxcdn.com/2/72x72/23f2.png'}
        draggable={false}
    />
);
