// @flow

import * as React from 'react';

export const MailboxWithMail = () => (
    <img
        style={{
            width: '1.25em',
            verticalAlign: '-0.25em',
        }}
        alt={'📬'}
        aria-label={'Emoji: Open mailbox with raised flag'}
        title={'Open mailbox with raised flag'}
        src={'https://twemoji.maxcdn.com/2/72x72/1f4ec.png'}
        draggable={false}
    />
);
