// @flow

import * as React from 'react';

export const CrossedFlags = () => (
    <img
        style={{
            width: '1.25em',
            verticalAlign: '-0.25em',
        }}
        alt={'🎌'}
        aria-label={'Emoji: Crossed flags'}
        title={'Crossed flags'}
        src={'https://twemoji.maxcdn.com/2/72x72/1f38c.png'}
        draggable={false}
    />
);
